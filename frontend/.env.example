# Backend API base URL for production deployments (e.g. Heroku, Railway, Render).
# In local development, leave this empty to use Vite's built-in dev proxy (/ -> http://localhost:5000).
# Example for production:
# VITE_API_BASE_URL=https://your-backend-app.herokuapp.com
VITE_API_BASE_URL=
