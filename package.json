{
  "name": "ai-knowledge-inbox-root",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal AI Knowledge Inbox - Full Stack Assignment",
  "scripts": {
    "prepare": "husky",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "typecheck:backend": "npm --prefix backend run typecheck",
    "typecheck:frontend": "npm --prefix frontend run typecheck",
    "typecheck": "npm run typecheck:backend && npm run typecheck:frontend",
    "lint": "eslint backend/src frontend/src",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "heroku-postbuild": "npm --prefix backend install && npm --prefix backend run build",
    "test:backend": "npm --prefix backend run test",
    "test": "npm run test:backend",
    "precommit": "npm run typecheck && npm run lint && npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.6",
    "globals": "^17.12.0",
    "husky": "^9.1.7",
    "typescript-eslint": "^8.70.0"
  }
}
